# Testbench

This directory contains testbenches for verifying the functionality of
the microcode-based MBIST controller.

The testbench initializes memory models, applies March sequences via
microcode execution, and checks pass/fail behavior and completion
signaling.
